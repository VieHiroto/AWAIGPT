# 📔 タスク・予定時間軸一元管理システム セットアップガイド（AW + GAS + Gemini無料版）

このセットアップは **Apple Watchショートカット → Googleスプレッドシート → GAS → Gemini（コピペ）→ Googleカレンダー** の半自動運用を前提にしています。

---

## 0. 完成状態（このガイドで目指す形）

- AWで1タップ起動し、音声でタスクを `タスク管理` シートへ追加
- スプレッドシートのメニューから「朝のGemini用テキスト生成」実行
- 生成テキストをGemini無料版に貼り付けてタイムラインを取得
- 返答を `スケジュール反映!A1` に貼り付けて「カレンダーへ反映」
- `【確定】` はスキップし、重複イベント作成を防止
- 未完了タスクは翌朝の生成時に自動で再対象化

---

## 1. スプレッドシート作成

新規スプレッドシートを作成し、以下4シートを作る。

1. `タスク管理`
2. `シフト一括登録`
3. `システム設定`
4. `スケジュール反映`

### 1.1 タスク管理

列定義:

- A: 完了（チェックボックス）
- B: タスク名
- C: 属性（💻PC作業 / 🏃‍♂️軽作業 / 🔒固定予定 など）
- D: 期限日（日時または `いつでも`）
- E: 想定時間(分)
- F: 分割数
- G: 1回あたり(分)
- H: Gemini用データ

式:

- `G2`:
  ```excel
  =IF(F2>0, E2/F2, E2)
  ```
- `H2`:
  ```excel
  =IF(A2=TRUE, "", "・"&IF(D2="いつでも", "期限：なし（空き時間おまかせ）", TEXT(D2,"m/dd")&"〆")&": "&B2&"【属性:"&C2&"】➔ 「"&G2&"分の作業コマ」を、"&IF(D2="いつでも", "直近の空き時間に", "期限までに")&IF(F2=1, "【一括（1回）で】", "【合計 "&F2&" 回】に分散して")&"配置せよ")
  ```

### 1.2 シフト一括登録

列定義:

- A: 日付
- B: 開始時間
- C: 終了時間
- D: シフト名

### 1.3 システム設定

`A:B` に以下を入力（A列がキー名）:

- `カレンダーID` : `primary`
- `稼働開始時間` : `09:00`
- `稼働終了時間` : `18:00`
- `前後バッファ（分）` : `15`
- `スキャン日数` : `3`

### 1.4 スケジュール反映

- `A1` にGeminiの回答（CSV形式）を貼る
- `B1` 以降はGAS処理で結果ログに利用

---

## 2. Apps Script導入

1. スプレッドシートで `拡張機能 > Apps Script`
2. `gas/Code.gs` の内容を貼り付け
3. 保存後、`onOpen` を一度実行して権限承認

### 2.1 メニュー

シートを再読み込みするとメニューが出る:

- `⚙️スケジュール管理 > 1. 朝のGemini用テキスト生成`
- `⚙️スケジュール管理 > 2. カレンダーへスケジュール反映`
- `⚙️スケジュール管理 > 3. シフト一括登録`

---

## 3. Apple Watch ショートカット設定

詳細は `shortcuts/README.md`。

最低限 `タスク登録` ショートカットを作り、以下を実装:

- 属性選択（💻 / 🏃‍♂️ / 🔒）
- タスク内容を音声入力
- 期限（日時 or いつでも）
- 想定時間（分）
- スプシ `タスク管理` に末尾追加

**必ず「Apple Watchに表示」をON**。

---

## 4. 毎朝運用

1. `タスク管理` の未完了を確認（必要なら分割数だけ調整）
2. メニュー `1. 朝のGemini用テキスト生成`
3. 出力テキストをGeminiへ貼り付け
4. Gemini返答（`日付, 開始-終了, タスク名`）を `スケジュール反映!A1` に貼る
5. メニュー `2. カレンダーへスケジュール反映`

---

## 5. Geminiへの指示テンプレート（GASが自動成形）

- 固定予定は動かさない
- 前後バッファ（分）を確保
- 稼働終了時刻以降は禁止
- 未完了タスクを期限逆算で割り当て
- 出力は必ず1行1予定のCSV:
  - `YYYY-MM-DD,HH:mm-HH:mm,タイトル`

---

## 6. 重複防止ルール

- タイトルに `【確定】` が含まれる行は登録しない
- 同一カレンダー内で「同日・同タイトル・同開始終了」が存在すれば作成しない

---

## 7. トラブル時チェック

- カレンダーIDが誤っていないか（`システム設定`）
- 時刻フォーマットが `HH:mm-HH:mm` か
- 日付が `YYYY-MM-DD` か
- Gemini出力が3カラムCSVになっているか
